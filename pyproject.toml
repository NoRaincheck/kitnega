[project]
name = "kitnega"
version = "0.1.0"
description = "Minimal Python workspace for agent tools"
requires-python = ">=3.14"
dependencies = ["carly", "cody", "duncan", "lib"]

[tool.uv.workspace]
members = ["carly", "cody", "duncan", "lib"]

[tool.uv.sources]
carly = { workspace = true }
cody = { workspace = true }
duncan = { workspace = true }
lib = { workspace = true }

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.13",
    "ty>=0.0.37",
]
