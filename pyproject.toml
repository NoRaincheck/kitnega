[project]
name = "kitnega"
version = "0.1.0"
description = "Minimal Python workspace for agent tools"
requires-python = ">=3.14"
dependencies = ["cody", "duncan"]

[tool.uv.workspace]
members = ["cody", "duncan"]

[tool.uv.sources]
cody = { workspace = true }
duncan = { workspace = true }

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.13",
    "ty>=0.0.37",
]
