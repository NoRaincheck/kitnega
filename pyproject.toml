[project]
name = "kitnega"
version = "0.1.0"
description = "Minimal Python workspace for agent tools"
requires-python = ">=3.14"
dependencies = ["carly", "cody", "duncan", "kenneth", "klaus", "lib", "merlin", "pixie", "raleigh"]

[tool.uv.workspace]
members = ["carly", "cody", "duncan", "kenneth", "klaus", "lib", "merlin", "pixie", "raleigh"]

[tool.uv.sources]
carly = { workspace = true }
cody = { workspace = true }
duncan = { workspace = true }
kenneth = { workspace = true }
klaus = { workspace = true }
lib = { workspace = true }
merlin = { workspace = true }
pixie = { workspace = true }
raleigh = { workspace = true }
pktpy = { git = "https://github.com/NoRaincheck/pktpy.git" }

[dependency-groups]
dev = [
    "lightgbm>=4.6.0",
    "nltk>=3.9.4",
    "onnx>=1.21.0",
    "onnxruntime>=1.26.0",
    "openml>=0.15.1",
    "pytest>=9.0.3",
    "pytest-playwright>=0.8.0",
    "ruff>=0.15.13",
    "scikit-learn>=1.9.0",
    "torch>=2.12.0",
    "ty>=0.0.37",
]

pktpy = ["pktpy"]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I001"]
